<h1 class="uk-margin-top">
  <%= t("tasks.edit.heading") %>
</h1>

<p>
  <%= @task.project.name %>
</p>

<%= render "form" %>

<p class="uk-margin-top">
  <%= link_to t("tasks.edit.back"), project_path(@task.project) %>
</p>
